// DEMO/DEVELOPMENT ONLY — replace with real authentication before production.
// Hardcoded here (not scattered across components) so it's easy to find and replace.
export const ADMIN_CREDENTIALS = {
  email: 'admin@awd.com',
  password: 'awd123',
};
